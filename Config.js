const config = {
  appName: 'chatops-knight-js',
  baseUrl: 'https://chatops-prod-int.extnet.ibm.com',
  timeout: 1000,
  proxy: false,
  rootPath: 'api/v1',
  ckHealth: 'ping'
};

module.exports = config;
